  <html>
  <head>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
      google.charts.load('current', {'packages':['corechart']});
      google.charts.setOnLoadCallback(drawChart);

      function drawChart() {
        var data = google.visualization.arrayToDataTable([
          ['Week', 'Company', 'Industry'],
          ['-3 Week',  parseFloat('<%= CompanySentiment3 %>'), parseFloat('<%= IndustrySentiment3 %>')],
          ['-2 Week',  parseFloat('<%= CompanySentiment2 %>'), parseFloat('<%= IndustrySentiment2 %>')],
          ['-1 Week',  parseFloat('<%= CompanySentiment1 %>'), parseFloat('<%= IndustrySentiment1 %>')]
        ]);

        var options = {
          title: 'Company Performance',
          curveType: 'function',
          legend: { position: 'bottom' }
        };

        var chart = new google.visualization.LineChart(document.getElementById('curve_chart'));

        chart.draw(data, options);
      }
    </script>
  </head>
  <body>
    <div id="curve_chart" style="width: 900px; height: 500px"></div>
  </body>
</html>